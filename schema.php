// Build/rebuild the database schema, optionally install test data too.
// Since running this script destroys the content of the database, Keep
// it above the root of the web app so that it can't be loaded in a browser.